import '../../data/models/motchill_play_models.dart';
import 'frame_player_stub.dart' as impl;

import 'package:flutter/widgets.dart';

Widget buildFramePlayer(PlaySource source) => impl.buildFramePlayer(source);
